{
  "name": "ns-featured-posts",
  "title": "NS Featured Posts",
  "version": "1.4.2",
  "license": "GPL-2.0+",
  "main_file": "ns-featured-posts.php",
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "deploy": "gulp deploy",
    "install-all": "composer install && yarn",
    "update-all": "composer update && yarn upgrade",
    "release": "yarn && composer install --no-dev --no-scripts -o && gulp deploy"
  },
  "repository": "github:ernilambar/ns-featured-posts",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-load-plugins": "^1.6.0",
    "gulp-plumber": "^1.2.1",
    "gulp-run": "^1.7.1",
    "gulp-zip": "^5.0.1",
    "node-wp-i18n": "^1.2.3"
  }
}
